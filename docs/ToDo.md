check timer sessions in admin panel
try 2 timer sessions at the same time
change profile specific info in settings



well VITE_API_BASE_URL has http, is that bad?
also if i add s to http would it suddently have https functionality?

in the diagram, the deploy arrow comes from to Web browser artifact to react spa artifact.
shouldn't the RESt API / HTTPS to backend come from the web browser instead of frontend server since the react app gets downloaded to device?
if im askked what is REST, what do i say?
can 2 designers startt a timer session at  the same time?